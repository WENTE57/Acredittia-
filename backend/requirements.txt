# --- API ---------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30
psycopg[binary]>=3.1
pydantic>=2.7
pydantic-settings>=2.2
pwdlib[argon2]>=0.2
PyJWT>=2.8
python-multipart>=0.0.9
email-validator>=2.1

# --- Cola de trabajos --------------------------------------------------
# Azure Cache for Redis. Con QUEUE_BACKEND=inproc no se usa, pero se instala
# igual para que una sola imagen sirva a la API, al worker y al beat.
celery[redis]>=5.4
redis>=5.0
# Cron en proceso para desarrollo (QUEUE_BACKEND=inproc)
APScheduler>=3.10

# --- Cifrado de credenciales de plataforma (JWE) ------------------------
cryptography>=42.0

# --- Azure -------------------------------------------------------------
# Identidad administrada para Blob Storage y Key Vault.
azure-identity>=1.17
azure-storage-blob>=12.20
azure-keyvault-keys>=4.9
azure-keyvault-secrets>=4.8

# --- Generación de reportes --------------------------------------------
openpyxl>=3.1

# --- Pruebas -----------------------------------------------------------
pytest>=8.2
httpx>=0.27
pgserver>=0.1.4     # PostgreSQL efímero para la suite (esquema real, no SQLite)
